global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'dwq-worker'
    static_configs:
      - targets: ['worker-else:8000']
    scrape_interval: 5s
    metrics_path: /
